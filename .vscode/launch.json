{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Graphite: Complete app",
      "type": "node-terminal",
      "request": "launch",
      "command": "bun run dev",
      "cwd": "${workspaceFolder}",
      "env": {
        "WEBKIT_DISABLE_DMABUF_RENDERER": "1"
      }
    }
  ]
}
